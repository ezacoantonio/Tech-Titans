import { makeStyles } from '@mui/styles';

const useWelcomePageStyles = makeStyles({
    root: {
        textAlign: 'center',
        padding: '20px',
        // Add more styles here
    },
    // You can add more style objects here if needed
});

export default useWelcomePageStyles;
